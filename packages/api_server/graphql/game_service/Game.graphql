#import 'external/User.graphql'
#import 'external/IdentityCard.graphql'

type Game {
  id: ID!

  players: [User!]!
  identities: [IdentityAssignments!]!

  dateCreated: DateTime!
  dateStarted: DateTime
  dateEnded: DateTime
}

type IdentityAssignments {
  playerId: ID!
  identityCard: IdentityCard!
}

scalar DateTime
