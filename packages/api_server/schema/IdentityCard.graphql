#import './Scalars.graphql'

enum IdentityType {
  Leader
  Guardian
  Assassin
  Traitor
}

type IdentityCard {
  id: ID!
  name: String!
  type: IdentityType!
  image: URL!
  text: String!
  rulings: [String!]!
  source: URL!
}
