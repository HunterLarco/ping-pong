#import './Book.graphql'

type Query {
  books(title: String, author: String): [Book!]
}

type AddBookMutationResponse {
  code: String!
  success: Boolean!
  message: String!
  book: Book
}

type Mutation {
  addBook(
    title: String!
    author: String!
    branch: String!
  ): AddBookMutationResponse!
}
