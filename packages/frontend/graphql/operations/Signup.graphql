mutation Signup($identityVerificationToken: ID!, $name: String!) {
  signup(
    request: {
      identityVerificationToken: $identityVerificationToken
      name: $name
    }
  ) {
    authToken
  }
}
